[project]
name = "distance-in-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ipyleaflet>=0.20.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.1.0",
    "uv>=0.9.18",
]
